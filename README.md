# 🏋️ Fitness Studio Booking API
## 🚀 Overview

This project is a backend API built using FastAPI for managing a fictional fitness studio.
The system allows users to:
- Register and authenticate using JWT
- Create fitness classes
- View upcoming classes
- Book available classes
- View their personal bookings

The API is designed with clean architecture, authentication, validation, and proper business logic handling.

## 🛠 Tech Stack

- Language: Python 3
- Framework: FastAPI
- Database: SQLite
- ORM: SQLAlchemy
- Authentication: JWT (JSON Web Tokens)
- Password Hashing: Passlib (bcrypt)
- API Docs: Swagger UI (auto-generated by FastAPI)

## 🏗 Project Structure

starlabs-booking-api/

│

├── app/

│   ├── main.py

│   ├── database.py

│   ├── models.py

│   ├── schemas.py

│   ├── auth.py

│   ├── dependencies.py

│   └── routers/

│         ├── users.py

│         ├── classes.py
  
│         └── bookings.py

│

├── requirements.txt

└── README.md

The project follows a modular structure separating:
- Models (database schema)
- Schemas (Pydantic validation)
- Authentication logic
- Business routes
- Database configuration

## 🔐 Authentication System

- JWT-based authentication
- Token expiration supported
- Passwords securely hashed using bcrypt
- Protected routes require Bearer token

Protected endpoints:
- POST /classes
- POST /book
- GET /bookings

## 📦 API Endpoints

*👤 Authentication*
Method	Endpoint	Description
POST	/signup	Register new user
POST	/login	Authenticate user & return JWT

*🧘 Classes*
Method	Endpoint	Description
POST	/classes	Create new fitness class (Auth required)
GET	/classes	View all upcoming classes

*📅 Bookings*
Method	Endpoint	Description
POST	/book	Book a class (Auth required)
GET	/bookings	View logged-in user’s bookings

## 🧠 Business Logic Implemented

The system handles important real-world constraints:

✔ Prevents overbooking
✔ Prevents duplicate bookings by same user
✔ Prevents booking past classes
✔ Deducts available slots on successful booking
✔ Validates required fields
✔ Returns proper HTTP status codes

## ⏰ Timezone Handling

All class times are stored in IST (Indian Standard Time).

Validation ensures past classes cannot be booked.

ISO datetime format used for API communication.

## ⚙️ Setup Instructions
1️⃣ Clone the repository
git clone <your_repo_url>
cd starlabs-booking-api

2️⃣ Create virtual environment
python -m venv venv

3️⃣ Activate environment

Windows:
venv\Scripts\activate

4️⃣ Install dependencies
pip install -r requirements.txt

5️⃣ Run the server
uvicorn app.main:app --reload

6️⃣ Access API Documentation

Open in browser:
http://127.0.0.1:8000/docs

**🧪 Example Request**
Create Class
POST /classes

{
  "name": "Yoga Flow",
  "date_time": "2026-06-15T10:00:00",
  "instructor": "John Doe",
  "available_slots": 20
}

## 👩‍💻 Author
**Harmeen Safoora**
Backend Developer Intern Applicant
